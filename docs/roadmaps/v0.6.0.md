# Provado Development Roadmap — v0.6.0

> **Status: IN PROGRESS.** Successor to `docs/roadmaps/v0.5.0.md` (shipped as tag `0.5.0`).
> Phase numbering restarts at Phase 1 for this version.

Primary source documents: `docs/ARCHITECTURE_DIRECTION_SOURCE.md` (rev v3, esp. Part D
"Diagnosis: what Provado does on top"), the signal catalog (`docs/simulations/Provado Signals
and diagnosis.md` + `… - Additional.md`), and the v0.5.0 Phase 6 refinements note.

> **Scope note (2026-06-30):** v0.5.0 delivered remote ingestion of four Magento operational
> signals (`cron_health`, `indexer_status`, `queue_backlog`, `config_change`) via the
> `ProvadoSignal` contract, plus the first dependency-graph diagnosis (`CronHealthPattern`).
> v0.5.0 left the **diagnosis layer deliberately crude**: a `max−min` dwell proxy that reports
> `0`, static thresholds instead of learned normal, no stuck-`running` detection, and a
> correlation window that holds many per-poll snapshots per `(signal, entity)`. v0.6.0 is the
> **dwell/baseline phase**: turn that raw stream into a learned, dwell-aware diagnosis. This is
> the layer the architecture doc calls the durable value — *"learn-normal baselines, dwell over
> state, collapse by Magento's dependency graph, and stamp onsets against the deploy/config
> timeline"* — the part generic APM does not ship.

## Status legend

- [ ] Not started
- [~] In progress
- [x] Done
- [!] Blocked

## Version goal

**Turn the raw operational-signal stream into a learned, dwell-aware diagnosis.** Reduce a window
of snapshots to current state per `(signal, entity)`; alarm on *how long* a contradiction has
persisted (real onset-based dwell, not a `max−min` proxy); learn what normal looks like per metric
instead of hard-coding thresholds; and collapse by Magento's explicit dependency graph, including
detecting a cron worker stuck in `running`.

The shipper stays dumb (raw state only); all of this new intelligence lives in Provado, on top of
the v0.5.0 ingestion seams (`SignalStore`, `CorrelationEngine`, `DiagnosticPattern`).

## v0.6.0 success definition

The system can, against the live lab:

1. Ship the operational signals **continuously** (scheduled, not on-demand), so the read window
   holds a real time series rather than a single hand-run snapshot.
2. Report **true onset-based dwell** for the cron signal (`cron_dwell_seconds` reflects how long
   cron has been degraded, computed by walking the series back to the onset) — not `0`, not
   `max−min`.
3. Fire `CronHealthPattern` on **deviation from a learned-normal baseline** (per-metric, from the
   series) rather than from a hard-coded threshold, with the static bound kept only as a safety
   floor.
4. Detect a **stuck-`running`** cron worker as a fire condition, and collapse downstream symptoms
   by an **explicit dependency graph** (cron upstream of cache / index / email / queue), lighting
   up the symptoms for the signals we ship today and declaring the edges for the ones we do not yet.

## Implementation rules

- **Provado stays remote.** No new direct access; the only new collection is scheduling the
  existing merchant-side shipper. Everything else reads `ProvadoSignal` via NerdGraph as before.
- **Ship raw state; diagnose in Provado.** Dwell, baselines, and graph collapse are added on the
  Provado side only — the shipper is unchanged.
- **Reuse the v0.1.0–v0.5.0 seams** (`SignalStore`/`SignalQuery`, `CorrelationEngine`,
  `CorrelationGroup`, `DiagnosticPattern`, `CronHealthPattern`). Dwell/baseline are new helpers on
  these seams, not a new transport or a parallel engine.
- **Stay narrow, one thing at a time.** Reduction + real dwell before baselines; baselines before
  the fuller graph. Don't build a baseline library before one learned metric works end to end.
- **Honest scope on the dependency graph.** Encode the full graph structure, but only activate
  downstream symptoms for signals that actually ship today (`indexer_status`, `queue_backlog`).
  `cache_validity` and an email/queue-consumer signal are deferred (they need the "Instrument"
  shipper) — they remain declared graph edges that light up when those signals exist.
- Every source-code phase is green on the lab server + a `/code-review` pass (per `CLAUDE.md`).

## Phase overview

| Phase | Status | Goal |
|---|---:|---|
| Phase 1 | [x] | Continuous shipping — schedule the php-agent shipper on the lab (+ docs) |
| Phase 2 | [x] | Dwell foundation — latest-per-`(signal,entity)` reduction + real onset-based dwell |
| Phase 3 | [ ] | Learned-normal baselines (replace static thresholds) |
| Phase 4 | [ ] | Full dependency graph + stuck-`running` detection |

---

## Phase 1 — Continuous shipping

**Status:** [x]

**Goal:** Make the operational signals flow continuously so the read window holds a real time
series — the precondition for any dwell computation. Operational + docs only; no `src/` change.

> Live on the lab: `provado-ship.php` is scheduled in root's crontab every 5 minutes
> (`*/5 * * * * MAGENTO_ROOT=/var/www/html/magento /usr/bin/php …/provado-ship.php`). Confirmed
> firing on the `12:25` boundary — one cron-driven run shipped 39 `ProvadoSignal` events
> (cron_health + 14 indexer + 23 queue + config_change) to the log, NR PHP extension enabled.
> Item 1 leaves no repo diff (the crontab lives on the lab); item 2 records the exact entry in
> `docs/shipping-methods.md` (Lab note) and `shippers/README.md`.

**Deliverables (items):**

1. [x] Schedule `shippers/php-agent/provado-ship.php` on the lab via OS-cron (every 5 minutes, NR
   PHP extension **enabled** — this is the shipping path, not the test path that disables it).
   Verified arriving via the cron-driven run.
2. [x] Update the lab/scheduling notes in `docs/shipping-methods.md` and `shippers/README.md` from
   "run on demand during development" to the scheduled reality, recording the exact cron entry.

## Phase 2 — Dwell foundation

**Status:** [x]

> Core diagnosis-layer change — **plan approval required** per `CLAUDE.md`.

**Goal:** Fix the two structural problems behind `cron_dwell_seconds = 0`: the window holds many
per-poll snapshots per `(signal, entity)`, and dwell is a within-group `max−min` proxy. Reduce to
current state, then compute dwell as the time since the *onset* of the current state.

> Live-validated on the lab end to end. With the shipper now continuous (Phase 1),
> `provado:diagnose` reports a real `cron_dwell_seconds` (4101 and climbing as the series
> accumulates) instead of the old `0`. A third fix was needed for the live value to be honest:
> the default operational NRQL was a bare `SELECT * FROM ProvadoSignal`, which NRQL truncates to
> the ~100 most-recent events and caps the series at a couple of polls — adding `LIMIT MAX`
> restored the full window (110 → 471 signals, 0 errors). Note the lab's cron is *persistently*
> degraded by the static `error > 500` bound (2791 historical errored `cron_schedule` rows), so
> every snapshot reads "bad" and dwell runs to the window edge — exactly the false-positive the
> Phase 3 learned baseline is meant to remove.

**Deliverables (items):**

1. [x] A **latest-per-`(signal, entity)` reduction** helper over a set/window of signals, so
   patterns reason over current state instead of a pile of duplicate snapshots. Unit-tested over a
   multi-poll series. (`src/Diagnosis/SignalSeries::latestPerEntity()`; `CronHealthPattern` reduces
   before reading downstream symptoms, so a stale bad snapshot can't flag a now-healthy entity.)
2. [x] **Onset-based dwell:** given a `(signal, entity)` series and a "is this state bad?"
   predicate, walk the series back to find when the current bad state began and return the elapsed
   seconds. Re-point `CronHealthPattern`'s `cron_dwell_seconds` at this so it reports true dwell
   (`SignalSeries::dwellSeconds()`, reusing `isCronUnhealthy` as the predicate). Also add `LIMIT
   MAX` to the operational NRQL so the series is not truncated. Live-verified on the lab.

## Phase 3 — Learned-normal baselines

**Status:** [ ]

> Core diagnosis-layer change — **plan approval required** per `CLAUDE.md`.

**Goal:** Replace the hard-coded `PENDING_BACKLOG = 100` / `ERROR_ELEVATED = 500` with a baseline
learned from the series, so slow drift is caught relative to normal — the architecture doc's
"learn normal … so a slow drift is caught before it trips a hard limit."

**Deliverables (items):**

1. A **baseline computation** per `(signal, entity, metric)` from the series (e.g. a robust
   central tendency + spread over a baseline window — median/percentile, not mean, to resist
   spikes). Unit-tested for the drift and the spike cases.
2. `CronHealthPattern` fires on **deviation from baseline** instead of the static threshold, with
   the static bound retained only as a floor so a cold start (no baseline yet) still alarms on an
   egregious value. Live-verify on the lab.

## Phase 4 — Full dependency graph + stuck-`running`

**Status:** [ ]

> Core pattern change — **plan approval required** per `CLAUDE.md`.

**Goal:** Make the dependency-graph collapse explicit and dwell-aware, and add the cron failure
shape v0.5.0 omitted: a worker stuck in `running`.

**Deliverables (items):**

1. **Stuck-`running` detection** as a fire condition: `running > 0` whose dwell exceeds the normal
   job runtime (from the Phase 3 baseline) is a dead worker, distinct from `missed` (scheduler not
   firing) and `pending` (backlog). Per the catalog, "an indexer/cron in `working`/`running` can be
   frozen, not busy."
2. An **explicit dependency graph** (cron upstream of cache / index / email / queue) replacing the
   ad-hoc inline checks in `downstreamSymptoms()`. Activate symptoms for the shipped signals
   (`indexer_status`, `queue_backlog`), dwell-qualified; declare the `cache`/`email` edges as
   awaiting their signals (deferred). Live-verify the collapsed verdict on the lab.

---

## Deferred (still out of scope — later versions)

- **More signals** (breadth): `cache_validity` (`getInvalidated()` — needs a CLI/module
  "Instrument" shipper, and is the natural first activator of the Phase 4 `cache` edge),
  `schema_drift`, `maintenance_flag`, order/payment integrity (#6 quote fingerprint, #7 payment
  transactions), inventory drift (#2), promotion (#3), per-region config (#10), cacheability (#12).
- **New Relic deploy markers** (`FROM Deployment` reader mode) — gated: the lab is Magento Open
  Source on-prem with no code deploys, so there is nothing to read; belongs to merchants on Adobe
  Commerce Cloud / with change tracking.
- Payment-integrity reconciliation against the PSP (Stripe/Adyen) directly.
- ERP (Odoo XML-RPC, `provado_erp_faults`) as a source.
- A productized Magento "Wire-Up" module (vs the config-driven shippers).
- Edge/CDN (Fastly/WAF) and measurement/consent (GA4) signals — gated, no lab infra.
